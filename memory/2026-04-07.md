# 2026-04-07 — Setup Session

## Memory Server Setup
- Instalado y configurado **openclaw-memory v0.1.0** (triple-layer: SQLite + Qdrant + PostgreSQL AGE)
- Docker containers: Qdrant (6333) + Apache AGE (5432)
- Servicio systemd: `openclaw-memory.service` en :7777

## Bugs fixeados
1. **bun:sqlite named params** — Bun 1.3.11 no soporta `:name` binding con objetos. Parche: convertir a positional `?` params
2. **Qdrant dimension mismatch** — Colección creada con 1536 dims, pero BGE-small produce 384. Recreada con dims correctas
3. **HuggingFace embeddings** — OpenAI client no compatible con HF API. Parche: fetch directo a `router.huggingface.co/hf-inference/models/`
4. **PostgreSQL config** — Puerto 5433→5432, contraseña `openclaw-memory` (no underscore), DB `agent_memory`

## Context window fix
- Modelo custom provider tenía 16k context → actualizado a 1M (1,048,576)
- maxTokens actualizado a 32k, reasoning habilitado

## Memories stored
- 9 memorias iniciales sobre Manuel, Ultron, sistema, infraestructura
- Todas con sync ok: SQLite + Qdrant + AGE
